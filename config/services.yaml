parameters:
    app_default_locale: '%env(string:APP_DEFAULT_LOCALE)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
